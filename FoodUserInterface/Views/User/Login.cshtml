@model FoodDatabase.fs_user
@{
    ViewBag.Title = "User Login";
}

<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
<script>
    $(document).ready(function () {
        $('#newUserBtn').click(function () {
            document.location.href = "Signup";
        });
    });
</script>

<style>
    .login-wrapper {
        border: 1px solid #e3e3e3;
        border-radius: 10px;
        width: 500px;
        padding: 50px;
        position: absolute;
        left: 50%;
        top: 50%;
        -webkit-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);
    }

    table {
        width: 100%;
    }

        table span {
            color: red;
            margin: 10px 0;
        }

        table td {
            padding: 10px 0;
        }
    center {
        margin: 20px 10px;
        width: 100%;
    }

    .center-flex {
        width: 100%;
        display: flex;
    }

    input {
        border: 1px solid #cbc9c8;
        padding: 5px;
        border-radius: 5px;
    }

        input[type = "text"], input[type = "password"] {
            width: 100%;
            height: 50px;
        }

        input[type = "submit"], #newUserBtn {
            
            background-color: #FFD371;
            border: none;
            color: black;
            font-weight: 500;
        }

            input[type = "submit"]:hover, #newUserBtn:hover {
                background-color: #FFB740;
                color: black;
            }
</style>

<div class="login-wrapper">
    @using (Html.BeginForm(FormMethod.Post))
    {
        <h2 style="text-align: left; margin-bottom: 30px;">User Log in</h2>
        <table>
            <tbody>
                <tr>
                    <td>@Html.LabelFor(model => model.u_username)</td>
                    <td>
                        @Html.TextBoxFor(model => model.u_username)<br />
                        <span>@Html.ValidationMessageFor(model => model.u_username)</span>
                    </td>
                </tr>
                <tr>
                    <td>@Html.LabelFor(model => model.u_password)</td>
                    <td>
                        @Html.PasswordFor(model => model.u_password) <br />
                        <span>@Html.ValidationMessageFor(model => model.u_password)</span>
                    </td>
                </tr>
            </tbody>
        </table>

        <div class="center-flex">
            <center>
                <input style="height: 40px; width: 100%; border-radius: 20px;" type="submit" class="btn btn-primary" value="Log in" name="name" />
            </center>
            <center>
                <input style="height: 40px; width: 100%; border-radius: 20px;" type="button" id="newUserBtn" class="btn btn-primary" value="Sign up" name="name" />
            </center>
        </div>

            <p style="color: red;">@ViewBag.error</p>
     }
</div>

